#include<iostream>
using namespace std;
int main()
{
    int l,w,h;
    cout<<"Enter the length,width and height of cubiod:";
    cin>>l>>w>>h; // l=length w=width h=height
    cout<<"Volume of cubiod is "<<l*w*h;
    return 0;
}